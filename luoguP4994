/* luoguP4994 */
/* 模拟 */
/**
 *斐波拉契数列的每一项对任意大于1的正整数M取模的时候，会产生循环
 *fib(n-1)modM 和 fib(n−2)modM最多只有M^2种取值，因此M^2次计算后一定会循环。
 *更一般地，我们可以证明，无论取什么模数M，最终模M下的斐波拉契数列都会是0,1,0,1……
 *输入M，求最小的n使得fib(n)modM=0,fib(n+1)modM=1。
 *题目很水，结论有点意思。
 **/
#include<cstdio>
#include<algorithm>
using namespace std;
int f[1000000]={0,1,1};
int main()
{
    int m;
    scanf("%d",&m);
    int a=1,b=1;
    for(int i=3;;i++)
    {
        a=(a+b)%m;
        if(b==0&&a==1)
        {
            printf("%d",i-1);
            break;
        }
        swap(a,b);
    }
    return 0;
}
